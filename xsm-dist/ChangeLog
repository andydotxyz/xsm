SNAPSHOT:

Features:
* Change rendering mechanism for XSM to use the site template directly
* Update News publishing to generate an archive and use a better URL scheme
* Add a "slug" option which allows custom URLs to be configured
* Add "required" option to form fields

Bug fixes:
* Fix styling of editing when final site is offline over over https

